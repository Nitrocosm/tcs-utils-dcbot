# Direct dependencies only. Transitive deps (aiohappyeyeballs, aiosignal,
# attrs, frozenlist, idna, multidict, propcache, yarl) are resolved by pip.
discord.py==2.6.4
python-dotenv==1.2.1
aiohttp==3.13.2
audioop-lts==0.2.2
